# Security and Privacy

ChannelDeck stores the IPTV password in the macOS Keychain and stores non-secret app preferences with `UserDefaults`.

Do not include provider credentials, playlist URLs, stream URLs, screenshots showing credentials, or private server details in bug reports.

The app is a client only. It does not provide IPTV content, subscriptions, credentials, or authorization to access streams.
